#include "main.h"

/**
 * _islower - Write a function that checks for lowercase character
 * @AP: execute alphabet
 * Return: 0 Always (successful)
 */

int  _islower(int AP)
{
	return (AP >= 'a' && AP <= 'z');
}
