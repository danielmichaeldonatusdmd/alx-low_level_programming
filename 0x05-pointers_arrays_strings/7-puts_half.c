#include "main.h"

/**
 * puts_half -  prints half of a string, followed by a new line.
 *
 * @s: input
 *
 * Return: 0
 */

void puts_half(char *str)
{
	int m;

	for (m = 0; s[m] != '\0'; m++)

	m++;
	for (m /= 2; s[m] != '\0'; m++)
	{
		_putchar(s[m]);
	}
	_putchar('\n');
}
